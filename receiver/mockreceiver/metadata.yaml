type: mock

status:
  class: receiver
  stability:
    development: [metrics]
  distributions: [contrib]
  codeowners:
    active: [hiwayama]

resource_attributes:
  mock.value:
    description: A value to set as a mock
    type: string
    enabled: true

metrics:
  value:
    enabled: true
    description: test value
    unit: s
    sum:
      value_type: int
      input_type: string
      monotonic: true
      aggregation_temporality: cumulative
    attributes: []